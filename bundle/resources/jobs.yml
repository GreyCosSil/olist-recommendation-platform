resources:
  jobs:
    tabela_teste:
      name: tabela-teste

      tasks:
        - task_key: tabela_teste

          notebook_task:
            notebook_path: ../notebooks/tabela_teste.py

              base_parameters:
                schema_name: ${var.schema_name}
                table_name: ${var.table_name}