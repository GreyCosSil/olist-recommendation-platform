bundle:
  name: olist-recommendation-platform

include:
  - resources/*.yml

sync:
  include:
    - ../notebooks/**

targets:
  dev:
    mode: development
    default: true

workspace:
  host: "https://{var.workspace_id}.cloud.databricks.com"