bundle:
  name: olist-recommendation-platform

include:
  - resources/*.yml

sync:
  include:
    - ../notebooks/**
variables:
  schema_name:
    default: teste_schema

  table_name:
    default: tabela_teste

targets:
  dev:
    mode: development
    default: true

workspace:
  host: "https://dbc-${workspace_id}.cloud.databricks.com"